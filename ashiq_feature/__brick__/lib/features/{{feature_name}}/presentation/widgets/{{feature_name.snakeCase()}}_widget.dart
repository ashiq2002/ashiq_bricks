import 'package:flutter/material.dart';

class {{feature_name.pascalCase()}}Widget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('{{feature_name}} Widget')));
  }
}